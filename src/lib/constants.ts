/** The URL of the Clubhouse website. */
export const WEBSITE_URL = import.meta.env.VITE_WEBSITE_URL || "https://clubhouse.test";

/** URL at which the Clubhouse API backend is available at. */
export const API_URL = import.meta.env.VITE_API_URL || "https://api.clubhouse.test";

/** URL at which the forum application is available at. */
export const FORUM_URL = import.meta.env.VITE_FORUM_URL || "https://forum.clubhuse.test";

/** The email address serving as the primary contact address.  */
export const CONTACT_ADDRESS = import.meta.env.VITE_CONTACT_ADDRESS || "human@clubhouse.test";
